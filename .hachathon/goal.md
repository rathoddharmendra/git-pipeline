# Week 1 goal: 

**FastAPI endpoint** that triggers a **shell script** and streams 5 fake progress steps **back over WebSocket** to a **React UI**

Goal extension:
    - use docker to run it in 2 instances - BE, UI using websocket on both ends
    - pushes to docker using git pipeline 
  
#### Note for self:
> creating mimic of service catalog (any design is fine)
> Not learning everything at one.. leave aside - docker, tf, service mesh, kafka, everything.
    - That “full cloud-native platform with service mesh + API gateway + Kafka + Terraform + UI” is a team-sized system. Trying to build it solo as your first execution project will keep you in that loop forever.
    - That's it. No Kafka. No service mesh. No Kubernetes yet. Just that. Once it works, everything else becomes an upgrade to something real — not another course.
    The regret you feel about Dell? That's actually your strongest asset. You know exactly what you want to build. Most people learning these technologies have no idea what problem they're solving. You already have the problem. Go build the solution.

    - Build what I know, 
      - Use minimal AI
      - Read lot of docs, and unblock for tasks
      - create to do for day to day execution.
      - finish ugly, but finish it to break that anxiety 👉 Real engineers ship small vertical slices, not perfect architectures.


    - I am going to copy, use AI, and read docs -- but complete this project with self control and architect it my design, without loosing control.
